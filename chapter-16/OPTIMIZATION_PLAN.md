# 第十六章优化计划：主播端架构

## 当前问题
1. 没有 SVG 架构图（虽然有 ASCII 图，但应该换成 SVG）
2. 代码过多（完整类定义和实现）
3. 理论部分薄弱（缺少架构设计原则说明）

## 优化方案

### 1. 新增 SVG 图（6个）
- `streamer-arch.svg` - 主播端整体架构图（替换 ASCII 图）
- `module-interfaces.svg` - 模块接口依赖关系图
- `thread-model.svg` - 线程模型与数据流
- `state-machine.svg` - 主播端状态机
- `error-recovery.svg` - 错误恢复流程图
- `av-sync.svg` - 音视频同步机制详解

### 2. 精简代码
- 删除 IVideoCapture 等完整接口定义（约 80 行）
- 删除 StreamerConfig 完整结构体（约 50 行）
- 删除 Pipeline 完整实现代码（约 100 行）
- 删除状态机完整实现（约 60 行）
- 保留核心设计思路和关键代码片段

### 3. 增强理论
- Pipeline 架构设计原则（单一职责、依赖倒置）
- 生产者-消费者模式在主播端的应用
- 状态机设计模式详解
- 错误处理策略（降级、重试、熔断）

### 4. 架构设计说明
- 为什么这样划分模块？
- 接口设计的权衡（灵活 vs 简单）
- 线程安全与性能的平衡
- 可测试性设计
